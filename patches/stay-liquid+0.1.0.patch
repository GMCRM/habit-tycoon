diff --git a/node_modules/stay-liquid/ios/Plugin/TabsBarOverlay.swift b/node_modules/stay-liquid/ios/Plugin/TabsBarOverlay.swift
index e983fbe..358676e 100644
--- a/node_modules/stay-liquid/ios/Plugin/TabsBarOverlay.swift
+++ b/node_modules/stay-liquid/ios/Plugin/TabsBarOverlay.swift
@@ -97,6 +97,14 @@ final class TabsBarOverlay: UIViewController, UITabBarDelegate {
         }
         tabBar.items = barItems
 
+        // Reassigning `.items` on an already-visible UITabBar doesn't always
+        // trigger a redraw of the button icons in place (e.g. going from the
+        // home icon to the plus icon on the center tab can silently keep the
+        // old glyph on screen). Force a layout pass so the new icons actually
+        // render immediately instead of waiting for some other UI event.
+        tabBar.setNeedsLayout()
+        tabBar.layoutIfNeeded()
+
         // Apply color configuration
         applyColorConfiguration()
 
